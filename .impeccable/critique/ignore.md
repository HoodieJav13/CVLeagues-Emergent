# Adjudicated detector findings — dropped from future critiques

- **Overused font (Inter)** — Inter/Oswald is the repository's locked
  typography token (CLAUDE.md); not a mockup-local choice.
- **Bounce or elastic easing (--ease-out-back)** — token consumed only by the
  celestial mark's transform; decorative motion on a decorative element.
- **Decorative grid-line background** — actually a 22px DOT field (detector
  misreads radial-gradient dots as hairlines); it is part of the loved v1-A
  identity, owner-ratified.
- **Layout property animation (transition: height on .dusk)** — the dusk-band
  reveal on mode flip; small masked element, deliberate theater. Revisit only
  if it reaches product code, where a transform/clip implementation is
  preferred.
